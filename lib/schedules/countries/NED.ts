import { DUTCH_EVENTS } from "@/lib/constants";
import { CountrySchedule } from "@/lib/schedules/types";

export const NED_SCHEDULE: CountrySchedule = {
  noc: "NED",
  name: "Netherlands",
  events: DUTCH_EVENTS,
};
