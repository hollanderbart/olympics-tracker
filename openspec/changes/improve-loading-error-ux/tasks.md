## 1. Loading States

- [ ] 1.1 Add `MedalOverviewSkeleton` component and render for medal pending state
- [ ] 1.2 Add `NextEventHighlightSkeleton` component and render for events pending state
- [ ] 1.3 Add `EventListSkeleton` component and render for events list pending state

## 2. Error and Retry

- [ ] 2.1 Add medals error panel with retry action wired to query `refetch`
- [ ] 2.2 Add events error panel with retry action wired to query `refetch`
- [ ] 2.3 Add tests for pending and error rendering paths

## 3. State Differentiation

- [ ] 3.1 Implement explicit pre-games detection path
- [ ] 3.2 Ensure pre-games message and network error message are mutually exclusive
